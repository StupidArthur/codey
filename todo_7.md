# TODO 7 — V1 最终收尾与开发关闭

> **Historical / completed:** TODO 7 已完成，V1 开发关闭。本文是关闭过程的任务记录；最终实现事实见 `docs/v1-acceptance.md` 和 `docs/architecture-review.md`。

## 0. 任务性质与结束规则

你是唯一执行 agent。本任务从 `81099cf` 开始，沿用 main-only 工作方式，不启动其他 agent。

这是最后一份 V1 开发任务单。目标是修复下面两个已确认的收尾缺口，交付可安装、可使用、结果诚实的 Windows V1，然后关闭开发。不是重新设计产品，也不是继续寻找可以优化的地方。

**任务范围和验收门在本文中冻结。全部通过后提交、推送并报告“V1 开发关闭”；不要自行提出 TODO 8，不要把可选改进变成新门禁。**

只有实际阻止本文场景、导致数据损坏、突破既定权限或泄露凭证的缺陷，才属于本轮必须修复的回归。其他发现只记录为非阻塞已知限制。外部服务、凭证、网络等真实阻碍必须明确报告，不能伪造通过，也不能用无限重试代替结论。

## 1. 先读代码，确认当前路径

先查看 `git status`、HEAD 和仓库适用的 AGENTS.md。保留用户已有改动。

按依赖顺序阅读：

1. `src/shared/contracts.ts`：Result、Verification、Loop decision 的持久化与展示契约。
2. `src/main/evidence/evidence.ts`、`EvidenceCollector.ts`、`VerificationExecutor.ts`：快照、检查事实、hash、检查权限。
3. `src/main/loop/LoopEvaluator.ts`、`LoopController.ts`、`ModelDecision.ts`：默认权限检查、脚本生成、证据失效、完成决策。
4. `src/main/rounds/RoundEngine.ts`、`src/main/result/ResultBuilder.ts`：Plan/Vibe/Loop 收尾、整轮输入与 Result。
5. renderer 中 Result 的实际组件、数据加载与展示路径：用 `rg` 定位，不只看类型定义。
6. `scripts/probes/loop-decision-gate.mjs`、`temporal-domain.mjs`、`installed-app-e2e.mjs`、`installed-app-ui.mjs`：复用真实路径与现有验收设施。

然后再看 `todo_6.md`、`docs/v1-acceptance.md`、`docs/architecture-review.md`，用于理解已经做出的决定，不能用文档声称通过代替代码与实跑。

保持已经成立的产品语义：自然语言 Spec 由模型理解，产品核实证据与反证；不恢复逐行正则证明全部需求的方案。

## 2. 收尾 A：让检查证据对应本次被检查的 workspace

### 2.1 当前具体问题

`LoopEvaluator` 在 `workspace-write` 下把 tests/typecheck/build 映射为读取 `temporal-verify/*.exit` 的内容检查，targets 仅包含退出码和日志文件。`runValid` 只比较这些 targets。

所以测试通过后源码又变了，即使没有重新执行测试，旧证据仍可能被当作当前通过。固定名称的旧 `.exit = 0` 也可能被下一轮误用。

读取退出码文件是产品直接核实的文件事实，不等于产品直接观察到子进程退出。必须准确表达这个来源。

### 2.2 必须实现的行为

1. 每次请求沙盒检查有独立执行身份。可采用 request id / nonce 和独立产物路径；不得把上一轮固定文件里的 `0` 当作本次检查通过。
2. 记录检查对应的 workspace 输入快照。默认可保守关联当前快照中的项目输入文件，复用现有采集范围和排除规则；排除本产品验证产物、Git 元数据、依赖目录和明确的构建输出。说明实际范围，不承诺检查所有可能的外部依赖。
3. 在请求检查前记录输入指纹；收到结果后核实输入没有变化。如果执行期间输入变化，证据不能支持当前完成，安排重跑。之后源码、测试、项目配置、依赖清单等被关联输入变化，旧通过必须失效。
4. 新增、删除、改内容都影响输入指纹。mtime 变化而内容相同可保持有效。检查本身产生的日志/退出码不能使自己失效，也不能被计为用户任务产物或持续进展。
5. 没有当前请求匹配的结果、结果格式异常、输入版本不匹配，都不能产生当前 `passed`。不要仅凭模型一句“跑过了”补造验证事实。
6. 重跑后的新有效结果可以取代同一检查对象的旧结果。保留历史，但旧失败或旧通过不得永久污染当前结论。
7. 持久化执行身份、输入指纹和证据来源所需的信息；重新打开历史 Result 后，仍能区分历史通过与当前有效通过。

### 2.3 关于信任边界：明确取舍，不开启新项目

V1 可以继续使用“产品生成 wrapper → DSH 沙盒执行 → 产品读取结果产物”路线。无需为这次收尾开发新的 OS 沙盒、私有 DSH 协议或不可伪造证明系统。

但 request id 只能防误用旧产物，**不能证明 agent 无法改写产物**。因此：

- 来源必须写为“DSH 沙盒检查报告；产品核实结果产物及输入快照”，不要写成“产品直接捕获子进程退出码”。
- 真正由产品直接执行的检查保留其真实 exit/signal 语义。
- UI 和文档不得把读取字符串 `0` 展示为独立观察到的进程退出。
- 代码任务的正常正例必须实际运行 wrapper 并通过真实检查；测试只写一个匹配产物不能代替这项端到端验收。
- 保留 `read-only` 与 `workspace-write` 权限边界；不能为拿到证据自动提升到 full-access 或绕过沙盒在宿主执行任意项目命令。

### 2.4 阶段 A 验收表

新增或扩展一个聚焦探针，使用实际 evaluator/controller/executor 路径：

| 场景 | 必须结果 |
| --- | --- |
| 真实检查通过，输入不变 | 当前有效通过，允许支持完成 |
| 检查通过后源码内容变化 | 旧证据失效，不能支持 completed |
| 检查通过后新增或删除项目输入 | 旧证据失效 |
| 检查通过后测试或项目配置变化 | 旧证据失效 |
| 仅日志/检查产物变化 | 不使项目输入失效，不算任务进展 |
| 上一 Round 遗留 `.exit = 0` | 不能满足本次请求 |
| 请求身份不匹配、缺失或异常 | 无当前通过证据 |
| 检查执行期间项目输入变化 | 本次结果失效，必须重跑 |
| 修改后重新真实检查通过 | 新证据有效，旧证据标历史 |
| 真实检查失败 | 不得 completed，不伪造通过 |

阶段记录要包含输入变化、证据 id/来源、有效性和最终状态；不记录凭证、原始对话或模型完整回答。

## 3. 收尾 B：Result 总结实际整轮成果

### 3.1 当前具体问题

`RoundEngine` 传入整轮信息只有 spec/outcome。`ResultBuilder.summarizeRound` 输出请求数量、成功数量、最后请求和文件数量。它没有表达这一轮实际做了什么。

另外，模型请求正常结束不能直接等同于需求成功；无验证不能通过空 Verification/Remaining 隐含“全部没问题”。

### 3.2 必须实现的行为

1. Result 的输入包含本轮全部相关请求和实际输出，结合整轮变更、已有验证、Loop decision、终态。Plan 使用本轮计划版本；Vibe 使用本轮全部 entries；Loop 使用 root Spec 和执行收尾信息。
2. Summary 描述实际成果、调整和范围。请求计数可以保留为次要信息，不能作为主要成果摘要。Vibe 两次完成不同事项，摘要应覆盖两项，不能只讲最后请求。
3. 可以使用模型整理摘要，也可以实现基于已有输出的确定性整理。采用最小可行方案，不新增长期任务系统。若调用模型，使用现有配置和只读整理路径；不允许摘要调用改 workspace，不增加可见 Round，必须有时间界限与失败回退。
4. 摘要整理失败不得阻止保存 Result、End Round 或释放 Session。回退应展示按请求组织的实际输出摘要，明确未进行综合整理。
5. 模型生成摘要只负责文字组织。Changes/Verification/终态由产品事实生成；摘要不能凭空新增 `passed`、抹掉失败或更改 Loop 状态。
6. Plan 表达计划成果，不能把计划中的实现说成已经实现。Vibe 的传输成功显示为请求执行结束，不用“几个成功”暗示功能验收通过。Loop 继续如实显示 completed/blocked/failed/budget_exhausted/interrupted。
7. 未运行验证时，Verification 明确显示“本轮未运行验证”；没有足够信息确认全部需求完成时，Remaining 表达“需求完成情况未独立确认”，不伪造具体缺陷。不要强制 Plan/Vibe 运行全部测试，也不要强制所有模式使用 Loop 的完成门禁。
8. 保持 Summary/Changes/Verification/Remaining 四部分。结果持久化后重新打开仍一致。旧数据兼容，无新字段时使用诚实回退，不重构旧 DSH 历史。

### 3.3 阶段 B 验收表

| 场景 | 必须结果 |
| --- | --- |
| Vibe 请求 A 完成事项甲，请求 B 完成事项乙，再 End Round | Summary 覆盖甲、乙，Changes 覆盖整轮 |
| 第二次请求调整第一次成果 | 摘要呈现最终调整，不机械列出互相矛盾的结论 |
| Plan 两个版本 | 表达最终计划与关键修订，不声称已实施 |
| Loop completed | 成果摘要、有效证据、真实终态一致 |
| Loop blocked/budget_exhausted | 显示已做部分和实际停止原因，不能说全部完成 |
| 没有任何验证运行 | Verification 明确未验证，不展示假 passed |
| 摘要整理异常 | Result 仍保存，回退可读，Session 可继续使用 |
| 重启并重新打开 Result | 四部分与持久化结果一致 |

至少一条探针从 RoundEngine 的整轮收尾进入，不仅直接构造 ResultBuilder 输入。测试关键信息的覆盖与事实边界，不要求模型输出逐字等于固定句子。

## 4. 最终集成验收：有限次数，完成即停止

先完成 A/B 的聚焦验收，再对最终候选版本执行下列门。不要每修一行就全量重跑。

### 4.1 编译与确定性回归

- `pnpm typecheck`
- `pnpm build`
- 阶段 A/B 的聚焦探针。
- `loop-decision-gate.mjs`
- `temporal-domain.mjs`
- `session-exclusivity.mjs`
- `git-evidence-delta.mjs`
- `verify-permissions-impl.mjs`

需要 `node:sqlite` 的探针按仓库现有方式用 Electron 的 Node 运行。每个命令必须真实结束，保存通过/失败与退出码；skip/partial 不能记为 passed。用例增加后不以旧检查数量为验收标准，以全部预期用例实际运行并通过为准。

### 4.2 Windows 安装后有限验收

执行 `pnpm dist:win`，安装最终构建。使用环境变量凭证，复用现有探针，调整旧断言以匹配准确的证据来源展示。

最终安装版本至少覆盖：

1. Plan 两次提交同一 Round、两个版本；切换 Vibe 后继续原 DSH Session。
2. Vibe 两次不同工作并 End Round，看到整轮成果和未验证说明；重启后 Result 保留。
3. 默认 workspace-write 下真实模型代码 Loop：实际运行沙盒检查，拿到当前输入对应证据，完成；安装后测试文件不可被探针偷偷篡改来制造通过。
4. 负例/半成品 Loop 如实停止，剩余事项明确；read-only 写入仍被拒绝。
5. 已有 DSH Session 发现并 resume、原上下文继承路径不回归。
6. 可见 UI 检查 Result 四部分、证据来源、历史验证标记、终态。复用现有自动化，只补受本轮影响的截图，不重做完整视觉设计。

复用 `installed-app-e2e.mjs`、`installed-app-ui.mjs`、`installed-app-legacy-e2e.mjs` 等。可以合并场景，避免重复消耗模型。正常全部通过后不再为“更放心”追加重复模型运行。失败只修复对应原因并重跑受影响门；若修复影响公共路径，再复跑相应回归。

## 5. 交付物与提交

1. A/B 的产品代码、必要契约兼容、聚焦探针。
2. 更新 `docs/v1-acceptance.md`：新增“TODO 7 最终关闭”，列出命令、退出码、脱敏结果、截图路径、最终证据信任边界。
3. 更新 `docs/architecture-review.md` 中实际受影响的证据/Result 描述。删除或纠正“产品直接获得沙盒进程退出码”等不准确表述，不重写全部文档。
4. 验收记录关联具体源码提交和安装包路径；列出非阻塞限制，明确其不代表本文验收未通过。开发关闭后新的功能和改善另行立项。
5. 扫描新增文件、日志、截图和 git diff，确保没有凭证、环境变量值、私人历史或原始模型对话。
6. main 上提交并推送 origin/main。不得 force push，不创建发布、不上游发 issue。若网络问题不能推送，明确区分“本地完成”与“已推送”。

## 6. 最终报告模板与关闭判据

报告只需：

```text
V1 开发关闭 / 或明确未通过的门
源码提交：
origin/main：
工作区状态：
安装包：

A：输入快照、请求身份、证据来源与失效行为 — 验收结果
B：整轮 Result、事实边界与异常回退 — 验收结果
编译/确定性回归：命令与退出码
安装后验收：实际运行场景与结果
证据文档/截图：路径
非阻塞已知限制：简短列出
```

**关闭判据：A/B 表中场景全部通过；4.1 全部通过；4.2 最终安装版本场景通过；文档、安装包和推送交付完成。满足后停止开发。**

不要把 SDK 未公开完整历史、没有挂机两小时、不能证明 agent 无法改写沙盒报告、UI 还可以更漂亮等已知边界扩展成新一轮。不要升级 DSH/Electron、增加产品功能、重构无关架构或建立新的抽象验证框架。

本任务收尾的是已冻结的 V1。通过后交给用户使用，本轮不再扩张需求。
